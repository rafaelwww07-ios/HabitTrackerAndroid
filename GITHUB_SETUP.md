# Инструкция по публикации на GitHub

Проект готов к публикации на GitHub. Выполните следующие шаги:

## 1. Создайте репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Заполните:
   - **Repository name**: `HabitTrackerAndroid` (или другое название)
   - **Description**: `Приложение для отслеживания привычек на Android`
   - Выберите **Public** или **Private**
   - **НЕ** добавляйте README, .gitignore или лицензию (они уже есть в проекте)
3. Нажмите **Create repository**

## 2. Подключите локальный репозиторий к GitHub

Выполните следующие команды в терминале в корне проекта:

```bash
# Добавьте remote репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/HabitTrackerAndroid.git

# Переименуйте основную ветку в main (если нужно)
git branch -M main

# Загрузите код на GitHub
git push -u origin main
```

## 3. Альтернативный способ (если git init не был выполнен)

Если репозиторий еще не инициализирован, выполните:

```bash
# Инициализация репозитория
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: Habit Tracker Android app"

# Добавление remote
git remote add origin https://github.com/YOUR_USERNAME/HabitTrackerAndroid.git

# Переименование ветки
git branch -M main

# Загрузка на GitHub
git push -u origin main
```

## 4. Обновите README.md

После создания репозитория обновите URL репозитория в README.md:
- Замените `yourusername` на ваш GitHub username в ссылках клонирования

## 5. Опционально: Добавьте теги релиза

```bash
# Создайте тег для версии 1.0
git tag -a v1.0 -m "Release version 1.0"

# Загрузите теги на GitHub
git push origin v1.0
```

## 6. Опционально: Настройте GitHub Pages

Если хотите добавить документацию:
1. Перейдите в Settings > Pages
2. Выберите source branch и папку
3. Нажмите Save

## Готово! 🎉

Ваш проект теперь доступен на GitHub. Не забудьте:
- Обновить описание репозитория
- Добавить темы (topics) в настройках репозитория (например: `android`, `kotlin`, `jetpack-compose`, `habit-tracker`)
- Добавить скриншоты приложения в README.md





